import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'images.pexels.com', 'img.freepik.com', 's.gravatar.com'],
  }
};

export default nextConfig;
